---
apiVersion: v1
kind: Service
metadata:
  name: onepassword-connect
  namespace: security
spec:
  type: ClusterIP
  selector:
    app: onepassword-connect
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
